package com.ncmine.importmine.ui.theme

import androidx.compose.ui.graphics.Color

// ==============================
// PALETA PRINCIPAL - NC IMPORT MINE
// Tema: Preto Profundo + Verde Neon
// ==============================

// Backgrounds
val NcBlackDeep    = Color(0xFF0A0A0A)   // Fundo principal
val NcBlackCard    = Color(0xFF111111)   // Cards
val NcBlackSurface = Color(0xFF161616)   // Superfície elevada
val NcBlackBorder  = Color(0xFF222222)   // Bordas sutis

// Verde Principal (Estilo Esmeralda Minecraft solicitado)
val NcGreenPrimary   = Color(0xFF3FB950)  // Verde #3FB950
val NcGreenNeon      = Color(0xFF3FB950)  // Mantendo compatibilidade com código existente
val NcGreenNeonDim   = Color(0xFF2E8B3F)  // Versão mais escura
val NcGreenNeonDark  = Color(0xFF1E5228)  // Fundo para botões/containers
val NcGreenNeonGlow  = Color(0x403FB950)  // Brilho
val NcGreenNeonLight = Color(0xCC3FB950)  // Translúcido

// Cores de status
val NcSuccess  = Color(0xFF00FF9F)   // Sucesso = verde neon
val NcError    = Color(0xFFFF4444)   // Erro = vermelho
val NcWarning  = Color(0xFFFFAA00)   // Aviso = âmbar
val NcInfo     = Color(0xFF00B4FF)   // Info = azul neon

// Textos
val NcTextPrimary   = Color(0xFFF0F0F0)  // Texto principal (quase branco)
val NcTextSecondary = Color(0xFF888888)  // Texto secundário (cinza)
val NcTextMuted     = Color(0xFF444444)  // Texto desativado
val NcTextNeon      = Color(0xFF00FF9F)  // Texto em destaque verde

// Cores de tipo de pacote
val PackColorResource = Color(0xFF00FF9F)  // Resource Pack → verde neon
val PackColorBehavior = Color(0xFF00B4FF)  // Behavior Pack → azul neon
val PackColorWorld    = Color(0xFFFF6B35)  // World Template → laranja
val PackColorSkin     = Color(0xFFFF00FF)  // Skin Pack → magenta
val PackColorUnknown  = Color(0xFF666666)  // Desconhecido → cinza

// Gradientes (usados em botões e headers)
val GradientNeonStart = Color(0xFF00FF9F)
val GradientNeonEnd   = Color(0xFF00CC7A)

// AdMob banner background
val AdBannerBackground = Color(0xFF0D0D0D)
